{
  "name": "fetch-all",
  "version": "1.0.0",
  "description": "Fetches all data based on react-router state.",
  "keywords": [
    "react",
    "react-router"
  ],
  "main": "build.js",
  "scripts": {
    "compile": "babel index.js -o build.js",
    "postinstall": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Romeo Van Snick <romeovs@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "all": "git://github.com/romeovs/all"
  }
}
